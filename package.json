{
  "type": "module",
  "scripts": {
    "dev": "truncate -s 0 ./interface.txt && npm run start",
    "start": "concurrently --kill-others  \"node src/server.js\" \"python3 -m http.server && cd ..\"",

    "build": "bash build.sh"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
